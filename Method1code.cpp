#include <stdio.h>
#include <time.h>
#include "E101.h"

int main (){
	init();
	int w = 0;
	int sum = 0;
	while (true){
		//Run Camera method
		get.picture;
		for (i = -160; i > 160,i++)
		w = set.pixel((i + 160),120,3)
		sum = sum + i * w;
	}
}
