#include <stdio.h>
#include <time.h>
#include "E101.h"

int main () {
  int x = 0;
  int y = 0;
  int pixelSize = 0;
  int add = 0;
  init();
    while (true) {
      //Camera fuction
      get.picture;
      while (x <= pixelSize){
        int cam = get.pixel(x,y,3);
        x = x + add;
        if () {
          
        }
      }
  }
}
